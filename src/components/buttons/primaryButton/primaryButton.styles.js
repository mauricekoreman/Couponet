import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 10,
    paddingVertical: 20,
    borderRadius: 100,
    borderWidth: 1,
    width: "80%",
    alignItems: "center",
    backgroundColor: "#FFF",
  },
});
